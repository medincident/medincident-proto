syntax = "proto3";

package zitadel.users.v1;

// Shared value types for the Zitadel user event passthrough tree.
// Mirrors zitadel/zitadel internal/domain types 1:1.

// Gender mirrors zitadel/zitadel internal/domain.Gender.
enum Gender {
  GENDER_UNSPECIFIED = 0;
  GENDER_FEMALE = 1;
  GENDER_MALE = 2;
  GENDER_DIVERSE = 3;
}
